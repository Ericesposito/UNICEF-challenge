// declare type for Organizations
export interface Organization {
  id: string;
  name: string;
}

// declaring a type for the DonationPageProps
export interface DonationPageProps {
  organizationId: string;
}
